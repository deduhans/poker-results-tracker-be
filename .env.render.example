# Render Environment Variables Example
# Copy these to your Render service environment variables

# Database Configuration (Render PostgreSQL)
POSTGRES_HOST=${RENDER_DB_HOST}
POSTGRES_PORT=${RENDER_DB_PORT}
POSTGRES_USER=${RENDER_DB_USER}
POSTGRES_PASSWORD=${RENDER_DB_PASSWORD}
POSTGRES_NAME=${RENDER_DB_NAME}

# Application Configuration
NEST_HOST=0.0.0.0
NEST_PORT=3000
PASSPORT_SECRET=your-secure-secret-key-here

# CORS Configuration - Update with your Netlify URL
CLIENT_HOST=your-app-name.netlify.app
CLIENT_PORT=443
